<h4 class="mb-3">Thông tin cá nhân admin</h4>

<div class="card">
  <div class="card-body">
    <div class="mb-3">
      <label class="form-label">Họ tên</label>
      <input type="text" class="form-control" value="<%= admin.fullName || '' %>" disabled>
    </div>

    <div class="mb-3">
      <label class="form-label">Email</label>
      <input type="text" class="form-control" value="<%= admin.email %>" disabled>
    </div>

    <div class="mb-3">
      <label class="form-label">Quyền</label>
      <input type="text" class="form-control" value="<%= admin.role %>" disabled>
    </div>

    <% if (admin.defaultAddress) { %>
      <div class="mb-3">
        <label class="form-label">Số điện thoại</label>
        <input type="text" class="form-control" value="<%= admin.defaultAddress.phone || '' %>" disabled>
      </div>
      <div class="mb-3">
        <label class="form-label">Địa chỉ</label>
        <textarea class="form-control" rows="2" disabled>
<%= (admin.defaultAddress.line1 || '') %>, 
<%= (admin.defaultAddress.ward || '') %>, 
<%= (admin.defaultAddress.district || '') %>, 
<%= (admin.defaultAddress.city || '') %>
        </textarea>
      </div>
    <% } %>

    <p class="text-muted mb-0" style="font-size: .9rem;">
      Đây là thông tin của tài khoản đang đăng nhập (admin), không phải khách hàng.
    </p>
  </div>
</div>
    